package logunov.maxim.domain.entity;

public enum ErrorType {
    INTERNET_IS_NOT_AVAILABLE,
    SERVER_IS_NOT_AVAILABLE,
    SERVER_ERROR,
    UNEXPECTED_ERROR,
    GPS_NOT_AVAILABLE_ERROR,
    NO_GPS_PERMISSION_ERROR
}
